[Unit]
Description=Ciferki - сервис количественного анализа
After=network.target

[Service]
User=CHANGE_ME
WorkingDirectory=/opt/ciferki
ExecStart=/opt/ciferki/.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
